//
//  {{ module_info.name }}{{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

final class {{ module_info.name }}DependenceProvider: IDependenceProvider {
	
	   internal static func loadDependences() {
        loadInteractors()
        loadRouters()
        loadViewControllers()
    }

    // MARK: - Intereactors

    private static func loadInteractors() {

    }

    // MARK: - Routers

    private static func loadRouters() {

    }

    // MARK: - HomeScreen dependencies

    private static func  loadViewControllers() {
        registerVCInjection({{ module_info.name }}VC.self) { vc in
            vc.router = resolve()
            vc.interactor = resolve()
        }
    }
}